FROM golang:1.25-alpine AS builder
WORKDIR /workspace/services/ocean-service
COPY contracts/go /workspace/contracts/go
COPY services/ocean-service/go.mod services/ocean-service/go.sum ./
RUN go mod download
COPY services/ocean-service .
RUN CGO_ENABLED=0 GOOS=linux go build -trimpath -ldflags="-s -w" -o /out/ocean-service ./cmd/service && \
    CGO_ENABLED=0 GOOS=linux go build -trimpath -ldflags="-s -w" -o /out/migrate ./cmd/migrate

FROM alpine:3.22 AS runtime
RUN apk add --no-cache ca-certificates && addgroup -S app && adduser -S -G app app
COPY --from=builder --chown=app:app /out/ocean-service /app/ocean-service
COPY --from=builder --chown=app:app /out/migrate /app/migrate
COPY --from=builder --chown=app:app /workspace/services/ocean-service/migrations /app/migrations
USER app:app
CMD ["/app/ocean-service"]
